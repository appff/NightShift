# NightShift Mission Configuration (v5.1 Project-Centric)

project:
  project_root: "." # The root directory for the project

mission:
  title: "Project-Specific Overrides Example"
  persona: "system-architect"
  constraints:
    - "Keep changes minimal and well-tested."

# Per-project overrides (optional)
brain:
  active_driver: "gemini"
hassan:
  active_driver: "claude"
critic:
  active_driver: "gemini"

tasks:
  - title: "Review the codebase and propose a refactor plan."
  - title: "Implement the refactor with tests."